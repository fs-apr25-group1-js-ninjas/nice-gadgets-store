import type { FC } from 'react';
import styles from './CheckoutSection.module.scss';

export const CheckoutSection: FC = () => {
  return <section className={styles.checkOut}>Checkout Section</section>;
};
